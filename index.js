'use strict';
const { WhatsBot, Location, Buttons, List, MessageMedia } = require('./src/index');
module.exports = {
  WhatsBot: WhatsBot,
  Location: Location,
  MessageMedia: MessageMedia,
  Buttons: Buttons,
  List: List
};